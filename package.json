{
  "name": "playwright-cli-claude",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright browser automation with Claude AI CLI",
  "scripts": {
    "build": "tsc",
    "test": "playwright test"
  },
  "devDependencies": {}
}
